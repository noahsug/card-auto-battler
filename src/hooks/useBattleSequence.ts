// import { useState } from 'react';

type Props = { turn: number; cardsPlayed: number };

export default function useBattleSequence({ turn, cardsPlayed }: Props) {
  //   const wait = useWait();
  //   timer.wait;
  // }
}
